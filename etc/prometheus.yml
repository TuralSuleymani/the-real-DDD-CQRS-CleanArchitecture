global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "flame.basket-service"
    static_configs:
      - targets: ["host.docker.internal:9464"]
        labels:
          service: "flame.basket-service"
